const express = require('express');
const app = express.Router();

app.get('/runtimeVersion', async function (req, res) {
    try {
        res.json(process.env.RUNTIME_VERSION);
    } catch (error) {
        console.log(error);
        res.status(204);
    }
});

module.exports = {
    app: app
}